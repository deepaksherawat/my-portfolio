name: Deploy WordPress Theme (Staging)

on:
  push:
    branches:
      - staging   # 👈 recommend: staging branch use karo

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy via SSH (SFTP)
        uses: appleboy/scp-action@v0.1.7
        with:
          host: 92.249.46.134
          username: u692901164
          password: ${{ secrets.SSH_PASSWORD }}
          port: 65002
          source: "."
          target: "/home/u692901164/domains/deepakwebdeveloper.com/public_html/staging/wp-content/themes/deepak-sherawat-web-developer/"
