name: Deploy WordPress Theme

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy via SSH (SFTP)
        uses: appleboy/scp-action@v0.1.7
        with:
          host: deepakwebdeveloper.com
          username: u692901164
          password: ${{ secrets.SSH_PASSWORD }}
          port: 22
          source: "."
          target: "/public_html/wp-content/themes/deepak-sherawat-web-developer/"
